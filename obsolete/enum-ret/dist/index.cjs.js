'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const ANSI = -1;
const JSON = 0;
const SAMPLES = 1;
const TABLE = 2;
/**
 *
 * @type {{TABLE: number, json: number, JSON: number, SAMPLES: number, ANSI: number, samples: number, table: number, ansi: number}}
 */

const ReT = {
  SAMPLES,
  JSON,
  TABLE,
  ANSI,
  samples: SAMPLES,
  json: JSON,
  table: TABLE,
  ansi: ANSI
};

exports.ReT = ReT;
