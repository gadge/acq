const ANSI = -1;
const JSON = 0;
const SAMPLES = 1;
const TABLE = 2;
const ReT = {
  SAMPLES,
  JSON,
  TABLE,
  ANSI,
  samples: SAMPLES,
  json: JSON,
  table: TABLE,
  ansi: ANSI
};

export { ReT };
