const SAMPLES = 0
const JSON = 1
const TABLE = 2
const ANSI = 3

const ReT = {
  SAMPLES,
  JSON,
  TABLE,
  ANSI,
  samples: SAMPLES,
  json: JSON,
  table: TABLE,
  ansi: ANSI,
}

export { ReT, SAMPLES, JSON, TABLE, ANSI }
